 you need to install avconv ffmpeg ffmpegtheora

- for that run aptitude install libav-tools ffmpeg  ffmpeg2theora
or 
- yum install  ffmpeg2theora ffmpeg 

for know which codec are installed  write 
-avconv -codecs

